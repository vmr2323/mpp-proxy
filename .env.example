# Required: Tempo wallet address that receives payments
RECIPIENT_ADDRESS=0xYourWalletAddressHere

# Optional: price per request in USDC.e (default: 0.01)
CHARGE_AMOUNT=0.01

# Optional: comma-separated allowed proxy domains
# ALLOWED_DOMAINS=api.coingecko.com,api.binance.com,api.kraken.com
